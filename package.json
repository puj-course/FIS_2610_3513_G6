{
  "name": "unimercs",
  "version": "1.0.0",
  "description": "Unimercs con patrones de diseño",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage --verbose",
    "test:ci": "jest --ci --coverage --testResultsProcessor jest-junit"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jest-junit": "^16.0.0"
  }
}
